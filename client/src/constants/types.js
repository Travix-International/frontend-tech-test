import PropTypes from 'prop-types';



const types = {
  _function: PropTypes.func.isRequired,
  _boolean: PropTypes.bool.isRequired,
  _number: PropTypes.number.isRequired,
  _taskArray: PropTypes.array.isRequired
}

export default types;
