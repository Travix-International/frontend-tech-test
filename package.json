{
  "name": "test-candidates",
  "version": "0.0.1",
  "description": "test back - front",
  "main": "server.js",
  "scripts": {
    "lint": "eslint ./*.js",
    "test": "yarn lint && mocha . --exit",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build-ui": "cd ui && yarn build",
    "test-ui": "cd ui && yarn test",
    "install-all": "yarn install && cd ui && yarn install"
  },
  "keywords": ["test", "travix"],
  "author": "spartans",
  "license": "ISC",
  "dependencies": {
    "express": "^4.14.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-http": "^3.0.0",
    "debug": "^3.1.0",
    "eslint": "^4.13.1",
    "eslint-config-travix": "^3.0.1",
    "eslint-plugin-import": "^2.8.0",
    "eslint-plugin-promise": "^3.6.0",
    "mocha": "^4.0.1",
    "nodemon": "^1.13.3"
  }
}
