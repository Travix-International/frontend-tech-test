{
  "name": "test-candidates",
  "version": "0.0.1",
  "description": "test back - front",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client && npm start",
    "predev": "node scripts/prepare-tasks.js ./tasks.json",
    "dev": "nodemon server.js & npm run client",
    "start": "node server.js"
  },
  "keywords": [
    "test",
    "travix"
  ],
  "author": "Ivan Burnaev",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "faker": "^4.1.0",
    "nodemon": "^1.19.1",
    "prettier": "1.18.2"
  }
}
