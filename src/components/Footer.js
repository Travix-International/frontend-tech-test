import React, { Component } from "react";

class Footer extends Component {

  render() {
    return (
      <footer>
        <p>Built with React, Redux, ES6, Express, SASS, Webpack, axios</p>
      </footer>
    );
  }
}

export default Footer;